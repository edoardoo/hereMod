alert("Holy doly, I work!");

function loadScript(url, callback)
{
    // Adding the script tag to the head as suggested before
    var head = document.getElementsByTagName('head')[0];
    var script = document.createElement('script');
    script.type = 'text/javascript';
    script.src = url;

    // Then bind the event to the callback function.
    // There are several events for cross browser compatibility.
    script.onreadystatechange = callback;
    script.onload = callback;

    // Fire the loading
    head.appendChild(script);
}

loadScript("https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js");
loadScript("https://gist.githubusercontent.com/edoardoo/2204168e8f0f46922d54/raw/50f3c6a4ede4436beaca4015468f1c614bc2fa03/gistfile1.txt");